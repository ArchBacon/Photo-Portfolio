<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="theme-color" content="#ffffff">
    <title>Website | {% block title %}Welcome!{% endblock %}</title>
    <link rel="apple-touch-icon" sizes="144x144" href="{{ asset('image/apple-touch-icon.png') }}">
    <link rel="icon" type="image/png" sizes="32x32" href="{{ asset('image/favicon-32x32.png') }}">
    <link rel="icon" type="image/png" sizes="16x16" href="{{ asset('image/favicon-16x16.png') }}">
    <link rel="manifest" href="{{ asset('image/site.webmanifest') }}">
    <link rel="mask-icon" href="{{ asset('image/safari-pinned-tab.svg') }}" color="#5bbad5">
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}

    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
        <script src="{{ asset('feather.min.js') }}"></script>
    {% endblock %}
</head>
<body class="bg-black font-montserrat text-white text-opacity-50 flex flex-col">
    {# TODO: Make header response #}
    {# TODO: Move header to own file/block #}
    <header>
        <a href="#" class="absolute top-3 left-4">
            <img src="{{ asset('image/logo.png') }}" class="h-10 md:h-28" alt="Logo">
        </a>

        <a href="#" class="absolute top-5 right-4 md:top-12 md:right-12"><i data-feather="menu" class="md:h-8 md:w-8"></i></a>
    </header>
    {% block body %}{% endblock %}
    {# TODO: Make footer response #}
    {# TODO: Move footer to own file/block #}
{#    {% block footer %}#}
{#        {% include 'shared/fixed-footer.html.twig' %}#}
{#    {% endblock%}#}

    <script>
        feather.replace()
    </script>
</body>
</html>
