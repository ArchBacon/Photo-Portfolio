<footer class="mt-4 mb-8 px-4 flex flex-col text-center w-full h-20 absolute bottom-0
        md:flex-row md:mb-0 md:justify-between">

    <div class="flex flex-row justify-center mb-8">
        {% for social in socials %}
            <a href="{{ social.url|default('#') }}" class="hover:text-white mx-5 h-min" title="{{ social.name }}"><i data-feather="{{ social.icon }}"></i></a>
        {% endfor %}
    </div>

    <p class="uppercase font-bold">Copyright &copy; 2020. <span class="inline-block">All Rights Reserved.</span></p>
</footer>